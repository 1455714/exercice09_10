<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mon Application</title>
</head>
<body>
  Node.js, Express et MongoDB.
  <button id='update'>Mise à jour téléphone</button>
    <form action="/adresse" method="POST">
    <input type="text" placeholder="nom" name="nom">
    <input type="text" placeholder="Telephone" name="telephone">
    <input type="text" placeholder="Ville" name="ville">
    <input type="text" placeholder="Code postal" name="code">
    <button type="submit">Submit</button>
  </form>

  <ul class="adresse">
  
  <% for(var i=0; i<adresse.length; i++) {%>
    <li class="client"><form action="/delete" method="POST">
      <span><%= adresse[i].nom %></span>
      <span><%= adresse[i].telephone %></span>
      <span><%= adresse[i].ville %></span>
      <span><%= adresse[i].code %></span>
      <input type="hidden" name="_id" value="<%= adresse[i]._id %>">
      <button type="submit">Delete</button></form>
      <form action="/formulaire" method="GET"><input type="hidden" name="i" value="<%= i %>"><button type="submit">Update</button></form>
    </li>
    
    
  <% } %>
  <button type="submit">Confirm</button>
  </form>
  </ul>
  <script type="text/javascript" src="modifier_adresse.js"></script>

</body>
</html>